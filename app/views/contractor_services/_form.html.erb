<%= form_for(@contractor_service) do |f| %>
  <% if @contractor_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contractor_service.errors.count, "error") %> prohibited this contractor_service from being saved:</h2>

      <ul>
      <% @contractor_service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
